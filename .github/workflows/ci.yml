name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    name: Build & test (JDK ${{ matrix.java }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java: [ 8, 11, 17, 21 ]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: maven

      - name: Start GeoServer + PostGIS (docker-compose)
        run: docker compose up -d

      - name: Wait for GeoServer to be ready
        # Checking /rest/about/version isn't enough: kartoza's image answers that as soon as
        # Tomcat deploys the webapp, but its background recipe scripts that set up the default
        # workspace/namespace can still be running for a bit after — WorkspaceManagerIntegrationTest
        # / NamespaceManagerIntegrationTest call getDefault() in their @BeforeAll and 404 if that
        # hasn't finished yet (confirmed on this repo's first CI run: 616/618 passed, only the two
        # setUp*() calls capturing the original default failed). Wait on /rest/workspaces/default
        # specifically instead.
        run: |
          for i in $(seq 1 60); do
            if curl -sf -u admin:geoserver http://localhost:8100/geoserver/rest/workspaces/default.json > /dev/null; then
              echo "GeoServer is up (default workspace configured)"
              exit 0
            fi
            echo "Waiting for GeoServer... ($i/60)"
            sleep 5
          done
          echo "GeoServer did not become ready in time"
          docker compose logs
          exit 1

      - name: mvn clean verify
        run: mvn --batch-mode --no-transfer-progress clean verify

      - name: Upload surefire reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports-jdk${{ matrix.java }}
          path: target/surefire-reports/
          retention-days: 14

      - name: Upload JaCoCo coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report-jdk${{ matrix.java }}
          path: target/site/jacoco/
          retention-days: 14

      - name: Stop docker-compose
        if: always()
        run: docker compose down -v
